---
phase: 01-foundation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/docbot/main.py
  - scripts/admin_override.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Fresh Docker deployments initialize the database automatically on first startup"
    - "Application lifespan calls init_db() before accepting requests"
    - "Admin override script uses timezone-aware datetime (no deprecation warnings)"
  artifacts:
    - path: "src/docbot/main.py"
      provides: "Application lifespan with database initialization"
      contains: "init_db"
    - path: "scripts/admin_override.py"
      provides: "Admin CLI with proper datetime usage"
      contains: "timezone.utc"
  key_links:
    - from: "src/docbot/main.py"
      to: "src/docbot/database.py"
      via: "init_db() called in lifespan startup"
      pattern: "await init_db\\(\\)"
---

<objective>
Close verification gap: Database initialization is missing from application lifespan. Fresh Docker deployments will fail because the database won't be created.

Purpose: The 01-02-PLAN.md required init_db() to be called in main.py lifespan (Task 1, step 5), but this was not implemented. This gap closure adds the missing call and fixes a minor deprecation issue.
Output: Working database initialization on app startup; clean datetime usage in admin script.
</objective>

<execution_context>
@C:\Users\KanaparthySaiSreekar\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\KanaparthySaiSreekar\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-VERIFICATION.md
@.planning/phases/01-foundation/01-02-PLAN.md
@src/docbot/main.py
@src/docbot/database.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add init_db() call to application lifespan</name>
  <files>src/docbot/main.py</files>
  <action>
    In src/docbot/main.py lifespan() function:

    1. Add import for init_db from docbot.database (alongside the existing imports inside lifespan)

    2. After `setup_logging(settings.app.log_level)` and before the yield statement, add:
       ```python
       # Initialize database schema (safe to run multiple times)
       try:
           await init_db()
           logger.info("Database initialized successfully")
       except Exception as e:
           logger.error(f"Database initialization failed: {e}")
           raise
       ```

    The init_db() function uses CREATE TABLE IF NOT EXISTS, so it's safe to run on every startup.
    The try/except ensures proper error logging if schema file is missing or database is inaccessible.

    DO NOT catch sqlite3 OperationalError separately - let any error propagate after logging.
    The app should fail to start if database initialization fails.
  </action>
  <verify>
    1. Delete docbot.db if it exists: `if exist docbot.db del docbot.db`
    2. Start the app briefly: `uv run python -c "from docbot.main import app; from fastapi.testclient import TestClient; c = TestClient(app); print(c.get('/health').json())"`
    3. Verify docbot.db was created: `if exist docbot.db echo Database created successfully`
    4. Verify tables exist: `uv run python -c "import sqlite3; c = sqlite3.connect('docbot.db'); tables = [t[0] for t in c.execute(\"SELECT name FROM sqlite_master WHERE type='table'\").fetchall()]; print(tables); assert 'appointments' in tables"`
  </verify>
  <done>Application lifespan calls init_db() on startup. Fresh deployments create the database automatically. Existing databases are unaffected (IF NOT EXISTS).</done>
</task>

<task type="auto">
  <name>Task 2: Fix deprecated datetime.utcnow() in admin_override.py</name>
  <files>scripts/admin_override.py</files>
  <action>
    In scripts/admin_override.py:

    1. Add `timezone` to the datetime import at line 12:
       Change: `from datetime import datetime`
       To: `from datetime import datetime, timezone`

    2. In log_action() function (line 58):
       Change: `timestamp = datetime.utcnow().isoformat() + "Z"`
       To: `timestamp = datetime.now(timezone.utc).isoformat()`

    3. In update_status() function (line 159):
       Change: `datetime.utcnow().isoformat()`
       To: `datetime.now(timezone.utc).isoformat()`

    This follows the same pattern used in main.py health_check() which already uses datetime.now(timezone.utc).
  </action>
  <verify>
    1. Run syntax check: `uv run python -m py_compile scripts/admin_override.py`
    2. Verify no deprecation warnings: `uv run python -W error::DeprecationWarning scripts/admin_override.py --help`
    3. Grep for utcnow: `findstr "utcnow" scripts\admin_override.py` -- should return no matches
  </verify>
  <done>Admin override script uses timezone-aware datetime.now(timezone.utc) instead of deprecated datetime.utcnow().</done>
</task>

</tasks>

<verification>
1. Fresh database creation works:
   - Delete docbot.db
   - Start app with TestClient
   - Verify docbot.db exists with all tables

2. No deprecation warnings in admin script:
   - `uv run python -W error::DeprecationWarning scripts/admin_override.py --help` succeeds

3. Existing tests still pass:
   - `uv run pytest tests/ -v`

4. App starts successfully with init_db() call:
   - Check startup log shows "Database initialized successfully"
</verification>

<success_criteria>
- init_db() is called in main.py lifespan after setup_logging()
- Database is created automatically on fresh deployments
- Admin script uses timezone-aware datetime (no utcnow())
- All existing tests pass
- Phase 1 verification gap closed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
