---
phase: 01-foundation
plan: 02
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/docbot/database.py
  - src/docbot/models.py
  - src/docbot/state_machine.py
  - src/docbot/timezone_utils.py
  - src/docbot/idempotency.py
  - db/001_initial_schema.sql
  - tests/test_state_machine.py
  - tests/test_timezone_utils.py
  - tests/test_idempotency.py
  - tests/conftest.py
autonomous: true

must_haves:
  truths:
    - "Appointment status transitions follow PENDING_PAYMENT -> CONFIRMED -> CANCELLED -> REFUNDED and reject invalid transitions"
    - "All timestamps are stored in UTC and can be converted to IST for display"
    - "Duplicate webhook event IDs are rejected without side effects"
    - "Slot locking uses database constraints, not in-memory locks"
    - "Database schema has indexes on all queryable columns"
  artifacts:
    - path: "db/001_initial_schema.sql"
      provides: "Complete SQLite schema with appointments, idempotency, slots tables"
      contains: "CREATE TABLE appointments"
    - path: "src/docbot/state_machine.py"
      provides: "Appointment state transition logic with validation"
      exports: ["AppointmentStatus", "transition", "can_transition"]
    - path: "src/docbot/timezone_utils.py"
      provides: "UTC/IST conversion utilities"
      exports: ["utc_now", "to_ist", "to_utc", "ist_now"]
    - path: "src/docbot/idempotency.py"
      provides: "Idempotency key checking and recording"
      exports: ["check_idempotency", "record_event"]
    - path: "src/docbot/database.py"
      provides: "Database connection and initialization"
      exports: ["get_db", "init_db"]
    - path: "tests/test_state_machine.py"
      provides: "Tests for state machine transitions"
    - path: "tests/test_timezone_utils.py"
      provides: "Tests for timezone conversions"
  key_links:
    - from: "src/docbot/state_machine.py"
      to: "src/docbot/database.py"
      via: "status updates use transition validation"
      pattern: "transition"
    - from: "src/docbot/main.py"
      to: "src/docbot/database.py"
      via: "init_db() called on startup"
      pattern: "init_db"
---

<objective>
Build the database schema, appointment state machine, timezone utilities, and idempotency framework using TDD. These are the data integrity foundations that every future phase depends on.

Purpose: DATA-01 through DATA-05, TIME-01 through TIME-05 require rigorous correctness. TDD ensures the state machine and timezone logic are provably correct before any feature depends on them.
Output: SQLite schema, tested state machine, timezone utilities, and idempotency module.
</objective>

<execution_context>
@C:\Users\KanaparthySaiSreekar\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\KanaparthySaiSreekar\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database schema and connection management</name>
  <files>db/001_initial_schema.sql, src/docbot/database.py, src/docbot/models.py, tests/conftest.py</files>
  <action>
    1. Create db/001_initial_schema.sql with the following tables:

       **appointments:**
       - id: TEXT PRIMARY KEY (UUID)
       - patient_phone: TEXT NOT NULL
       - patient_name: TEXT NOT NULL
       - patient_age: INTEGER NOT NULL
       - patient_gender: TEXT NOT NULL (CHECK IN ('Male', 'Female', 'Other'))
       - consultation_type: TEXT NOT NULL (CHECK IN ('online', 'offline'))
       - appointment_date: TEXT NOT NULL (ISO date YYYY-MM-DD)
       - slot_time: TEXT NOT NULL (HH:MM format)
       - status: TEXT NOT NULL DEFAULT 'PENDING_PAYMENT'
       - razorpay_payment_id: TEXT
       - razorpay_order_id: TEXT
       - razorpay_refund_id: TEXT
       - google_calendar_event_id: TEXT
       - google_meet_link: TEXT
       - language: TEXT NOT NULL DEFAULT 'en'
       - created_at: TEXT NOT NULL (ISO 8601 UTC)
       - updated_at: TEXT NOT NULL (ISO 8601 UTC)
       - cancelled_at: TEXT
       - refunded_at: TEXT

       **slot_locks:**
       - appointment_date: TEXT NOT NULL
       - slot_time: TEXT NOT NULL
       - locked_by_phone: TEXT NOT NULL
       - locked_until: TEXT NOT NULL (UTC timestamp)
       - PRIMARY KEY (appointment_date, slot_time)
       DATA-02: This table enforces database-level slot locking via PRIMARY KEY constraint.

       **idempotency_keys:**
       - event_id: TEXT PRIMARY KEY
       - source: TEXT NOT NULL (e.g. 'razorpay', 'whatsapp')
       - processed_at: TEXT NOT NULL (UTC timestamp)
       - result: TEXT (JSON string of the processing result)
       DATA-03, DATA-04: Prevents duplicate webhook processing.

       **prescriptions:**
       - id: TEXT PRIMARY KEY (UUID)
       - appointment_id: TEXT NOT NULL REFERENCES appointments(id)
       - medicines: TEXT NOT NULL (JSON)
       - instructions: TEXT
       - pdf_path: TEXT
       - created_at: TEXT NOT NULL (UTC)
       - UNIQUE(appointment_id) -- one prescription per appointment

       **Indexes (aggressive indexing per CONTEXT decisions):**
       - idx_appointments_date ON appointments(appointment_date)
       - idx_appointments_phone ON appointments(patient_phone)
       - idx_appointments_status ON appointments(status)
       - idx_appointments_date_status ON appointments(appointment_date, status)
       - idx_appointments_date_slot ON appointments(appointment_date, slot_time)
       - idx_slot_locks_until ON slot_locks(locked_until)
       - idx_idempotency_source ON idempotency_keys(source)

    2. Create src/docbot/database.py:
       - get_db() async function returning aiosqlite connection
       - init_db() function that reads and executes db/001_initial_schema.sql
       - Use WAL mode for SQLite (better concurrent read performance)
       - Enable foreign keys
       - Use BEGIN IMMEDIATE for write transactions (DATA-01)
       - Database path from Settings.database.path

    3. Create src/docbot/models.py with Pydantic models:
       - Appointment (all fields from table)
       - SlotLock
       - IdempotencyKey
       - Prescription
       These are data transfer objects, not ORM models.

    4. Create tests/conftest.py with:
       - Fixture that creates an in-memory SQLite database for tests
       - Fixture that initializes schema on the test DB
       - Fixture that provides async test client for the FastAPI app

    5. Update main.py lifespan to call init_db() on startup.
    6. Update /ready endpoint to include database check: try connecting and running SELECT 1.
  </action>
  <verify>
    Run: `uv run python -c "import asyncio; from docbot.database import init_db; asyncio.run(init_db())"` -- should create docbot.db with all tables. Then verify: `uv run python -c "import sqlite3; c = sqlite3.connect('docbot.db'); print([t[0] for t in c.execute('SELECT name FROM sqlite_master WHERE type=\"table\"').fetchall()])"` should list appointments, slot_locks, idempotency_keys, prescriptions.
  </verify>
  <done>SQLite schema created with all tables, indexes, and constraints. Database connection management works with WAL mode. Test fixtures provide in-memory DB for testing.</done>
</task>

<task type="auto">
  <name>Task 2: TDD -- State machine, timezone utilities, and idempotency</name>
  <files>src/docbot/state_machine.py, src/docbot/timezone_utils.py, src/docbot/idempotency.py, tests/test_state_machine.py, tests/test_timezone_utils.py, tests/test_idempotency.py</files>
  <action>
    Follow RED-GREEN-REFACTOR cycle for each module:

    **State Machine (DATA-05):**

    RED: Create tests/test_state_machine.py with these test cases:
    - test_valid_transitions: PENDING_PAYMENT->CONFIRMED, CONFIRMED->CANCELLED, CANCELLED->REFUNDED
    - test_invalid_transitions: PENDING_PAYMENT->CANCELLED (skip), PENDING_PAYMENT->REFUNDED (skip), CONFIRMED->PENDING_PAYMENT (backward), REFUNDED->CONFIRMED (backward)
    - test_can_transition_returns_bool: can_transition("CONFIRMED", "CANCELLED") -> True, can_transition("CONFIRMED", "REFUNDED") -> False
    - test_all_statuses_defined: AppointmentStatus enum has exactly 4 values
    - test_transition_raises_on_invalid: transition with invalid status raises ValueError

    GREEN: Create src/docbot/state_machine.py:
    - AppointmentStatus enum: PENDING_PAYMENT, CONFIRMED, CANCELLED, REFUNDED
    - VALID_TRANSITIONS dict mapping current -> set of allowed next states
    - can_transition(current: str, target: str) -> bool
    - transition(current: str, target: str) -> str (returns target if valid, raises ValueError if not)

    **Timezone Utilities (TIME-01 through TIME-05):**

    RED: Create tests/test_timezone_utils.py with:
    - test_utc_now_returns_utc: utc_now() has UTC timezone
    - test_to_ist_converts_correctly: known UTC time converts to IST (+5:30)
    - test_to_utc_converts_correctly: known IST time converts to UTC (-5:30)
    - test_ist_now_returns_ist: ist_now() has Asia/Kolkata timezone
    - test_format_ist_display: format_ist(utc_dt) returns "DD Mon YYYY, HH:MM AM/PM IST"
    - test_is_same_day_ist: two UTC timestamps that are same day in IST return True
    - test_slot_time_to_utc: given date "2026-02-05" and slot "14:30" (IST), converts to correct UTC datetime

    GREEN: Create src/docbot/timezone_utils.py:
    - IST = ZoneInfo("Asia/Kolkata") constant
    - utc_now() -> datetime (UTC)
    - ist_now() -> datetime (IST)
    - to_ist(utc_dt: datetime) -> datetime
    - to_utc(ist_dt: datetime) -> datetime
    - format_ist(utc_dt: datetime) -> str (human readable IST string)
    - is_same_day_ist(dt1: datetime, dt2: datetime) -> bool
    - slot_to_utc(date_str: str, time_str: str) -> datetime (treats input as IST, returns UTC)

    **Idempotency (DATA-03, DATA-04):**

    RED: Create tests/test_idempotency.py with:
    - test_new_event_not_duplicate: check_idempotency for new event_id returns False
    - test_recorded_event_is_duplicate: after record_event, check_idempotency returns True
    - test_record_stores_result: recorded event can be retrieved with its result
    - test_different_sources_independent: same event_id from different sources tracked independently (actually, event_id is globally unique, test that same ID is always duplicate regardless of source)

    GREEN: Create src/docbot/idempotency.py:
    - check_idempotency(db, event_id: str) -> bool (True if already processed)
    - record_event(db, event_id: str, source: str, result: dict | None = None) -> None
    - These use the idempotency_keys table

    Run all tests: `uv run pytest tests/ -v`
  </action>
  <verify>
    Run: `uv run pytest tests/test_state_machine.py tests/test_timezone_utils.py tests/test_idempotency.py -v` -- all tests pass.
  </verify>
  <done>State machine rejects invalid transitions and accepts valid ones. Timezone utilities correctly convert between UTC and IST. Idempotency framework detects and rejects duplicate events. All verified by passing tests.</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/ -v` -- all tests pass
2. Database schema creates successfully with all tables and indexes
3. State machine: PENDING_PAYMENT->CONFIRMED->CANCELLED->REFUNDED works; invalid transitions raise ValueError
4. Timezone: utc_now() returns UTC, to_ist() adds +5:30, slot_to_utc() converts IST slot to UTC
5. Idempotency: duplicate event_id detected, new event_id allowed
6. /ready endpoint now checks database connectivity
</verification>

<success_criteria>
- All state machine transitions tested and correct (DATA-05)
- UTC/IST conversions provably correct via tests (TIME-01)
- Slot-to-UTC conversion handles IST correctly (TIME-02, TIME-05)
- Idempotency check works for webhook deduplication (DATA-03, DATA-04)
- Database schema has all tables with indexes (DATA-02)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
