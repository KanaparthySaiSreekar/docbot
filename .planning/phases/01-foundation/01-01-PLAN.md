---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/docbot/__init__.py
  - src/docbot/main.py
  - src/docbot/config.py
  - src/docbot/logging_config.py
  - config.example.json
  - .gitignore
  - .env.example
autonomous: true

must_haves:
  truths:
    - "FastAPI app starts and responds to HTTP requests"
    - "Config loads from config.test.json or config.prod.json based on DOCBOT_ENV"
    - "Structured JSON logs written to stdout on every request"
    - "/health returns 200 with liveness status"
    - "/ready returns 200 when app dependencies are available"
  artifacts:
    - path: "pyproject.toml"
      provides: "UV project definition with all Phase 1 dependencies"
      contains: "fastapi"
    - path: "src/docbot/main.py"
      provides: "FastAPI application with health endpoints and logging middleware"
      exports: ["app"]
    - path: "src/docbot/config.py"
      provides: "Configuration loader with complete schema"
      exports: ["Settings", "get_settings"]
    - path: "src/docbot/logging_config.py"
      provides: "Structured JSON logging setup"
      exports: ["setup_logging"]
    - path: "config.example.json"
      provides: "Example configuration with all fields documented"
  key_links:
    - from: "src/docbot/main.py"
      to: "src/docbot/config.py"
      via: "get_settings() on startup"
      pattern: "get_settings"
    - from: "src/docbot/main.py"
      to: "src/docbot/logging_config.py"
      via: "setup_logging() on startup"
      pattern: "setup_logging"
---

<objective>
Scaffold the DocBot FastAPI project with UV package manager, complete configuration system, structured JSON logging, and health check endpoints.

Purpose: This is the foundation every other plan depends on. Without the app scaffold, config loading, and logging, nothing else can be built.
Output: A running FastAPI app with config, logging, and health endpoints.
</objective>

<execution_context>
@C:\Users\KanaparthySaiSreekar\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\KanaparthySaiSreekar\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold FastAPI project with UV and dependencies</name>
  <files>pyproject.toml, src/docbot/__init__.py, src/docbot/main.py, .gitignore, .env.example</files>
  <action>
    1. Initialize a UV project in the repo root with `uv init --lib` or manually create pyproject.toml.
    2. pyproject.toml must include:
       - name: "docbot"
       - python requires: ">=3.11"
       - Dependencies: fastapi, uvicorn[standard], python-jose[cryptography], authlib, httpx, itsdangerous, aiosqlite, python-multipart, jinja2
       - Dev dependencies: pytest, pytest-asyncio, httpx (for test client)
    3. Create src/docbot/__init__.py (empty or with version).
    4. Create src/docbot/main.py with:
       - FastAPI app instance with title="DocBot API"
       - Lifespan context manager that calls config loading and logging setup on startup
       - GET /health endpoint: returns {"status": "alive", "timestamp": "<utc_iso>"}
       - GET /ready endpoint: returns {"status": "ready", "checks": {"config": true}} — will add DB check in Plan 02
       - Request logging middleware: log method, path, status_code, duration_ms as JSON for every request
    5. Create .gitignore: __pycache__, .venv, *.pyc, config.prod.json, config.test.json, *.db, .env, dist/, *.egg-info
    6. Create .env.example with DOCBOT_ENV=test (the only env var, controls which config file to load)
    7. Run `uv sync` to install all dependencies.
  </action>
  <verify>
    1. Start the server with a timeout so it auto-exits: `uv run python -c "import uvicorn; uvicorn.run('docbot.main:app', host='0.0.0.0', port=8000, timeout_keep_alive=5)"` in a separate terminal (or use `Start-Process` on Windows).
    2. Alternatively, verify non-interactively: `uv run python -c "from docbot.main import app; from fastapi.testclient import TestClient; c = TestClient(app); r = c.get('/health'); print(r.status_code, r.json()); assert r.status_code == 200 and r.json()['status'] == 'alive'"` -- this avoids backgrounding entirely by using FastAPI's TestClient.
    3. Also verify /ready: `uv run python -c "from docbot.main import app; from fastapi.testclient import TestClient; c = TestClient(app); r = c.get('/ready'); print(r.status_code, r.json()); assert r.status_code == 200"`
  </verify>
  <done>FastAPI app starts, /health returns 200, /ready returns 200, request logging emits structured JSON to stdout.</done>
</task>

<task type="auto">
  <name>Task 2: Configuration system with complete schema and environment separation</name>
  <files>src/docbot/config.py, config.example.json</files>
  <action>
    1. Create src/docbot/config.py with a Pydantic Settings model (`Settings`) that defines the COMPLETE configuration schema for the entire project (not just Phase 1). This prevents schema changes in later phases.

    2. Settings schema must include these sections (use nested Pydantic models):

       **clinic** (CONF-01, CONF-02):
       - name: str
       - address: str (full clinic address for WhatsApp messages)
       - phone: str
       - doctor_name: str
       - doctor_degree: str (e.g. "MBBS")
       - doctor_registration: str (medical council registration number)
       - signature_image_path: str (path to signature PNG, CONF-03)

       **schedule** (for future BOOK-06, BOOK-07):
       - working_days: list[int] (0=Monday through 6=Sunday)
       - start_time: str (HH:MM format, e.g. "09:00")
       - end_time: str (HH:MM format, e.g. "17:00")
       - break_start: str (HH:MM, e.g. "13:00")
       - break_end: str (HH:MM, e.g. "14:00")
       - slot_duration_minutes: int = 15
       - max_appointments_per_day: int = 50

       **fees**:
       - online_consultation: int = 500 (in INR)

       **database**:
       - path: str = "docbot.db"

       **auth** (for AUTH-01, AUTH-02):
       - google_client_id: str
       - google_client_secret: str
       - session_secret_key: str (for signing session cookies)

       **whatsapp** (for future phases):
       - phone_number_id: str
       - access_token: str
       - verify_token: str
       - api_version: str = "v21.0"

       **razorpay** (for future phases):
       - key_id: str
       - key_secret: str
       - webhook_secret: str

       **google_calendar** (for future phases):
       - credentials_path: str
       - calendar_id: str

       **app**:
       - env: str = "test" (test or prod)
       - base_url: str (e.g. "https://docbot.example.com")
       - timezone: str = "Asia/Kolkata" (CONF-04)
       - debug: bool = True
       - log_level: str = "DEBUG"

    3. Create `get_settings()` function that:
       - Reads DOCBOT_ENV from environment (defaults to "test")
       - Loads config.{env}.json (e.g. config.test.json)
       - If file missing: logs warning, uses defaults where possible
       - If file has invalid fields: logs warning per field, uses defaults
       - Caches the result (singleton pattern with functools.lru_cache)
       - Validates the loaded config against the Pydantic model

    4. Create config.example.json with ALL fields populated with example/placeholder values and comments explaining each. Mark secrets with "CHANGE_ME" placeholder.

    5. Ensure DEPLOY-01 (separate configs) and DEPLOY-02 (test/live keys never mixed) are satisfied by the env-based file loading approach.
  </action>
  <verify>
    Create a minimal config.test.json with placeholder values. Run: `DOCBOT_ENV=test uv run python -c "from docbot.config import get_settings; s = get_settings(); print(s.clinic.name, s.app.env)"` -- should print the clinic name and "test".
  </verify>
  <done>Config loads from environment-specific JSON file, warns on missing/invalid fields with graceful defaults, complete schema covers all future phases, config.example.json documents every field.</done>
</task>

<task type="auto">
  <name>Task 3: Structured JSON logging with request correlation</name>
  <files>src/docbot/logging_config.py</files>
  <action>
    1. Create src/docbot/logging_config.py with:
       - setup_logging(log_level: str) function that configures Python's logging module
       - Use python-json-logger or manual JSON formatter to output one JSON object per line
       - Each log line must include: timestamp (ISO 8601 UTC), level, logger_name, message, and any extra fields
       - Add a request_id field using a context variable (contextvars) for request correlation
       - Log level configurable from Settings.app.log_level
       - Suppress noisy uvicorn access logs (uvicorn.access) since we have our own request middleware

    2. OPS-01 compliance: All application logs are structured JSON.
    3. OPS-02 foundation: Error-level logs will be used for alerting in Plan 04. For now, ensure error logs include sufficient context (exception type, traceback, relevant IDs).

    4. Update main.py to:
       - Call setup_logging() during lifespan startup
       - Generate a unique request_id per request in the middleware
       - Include request_id in all log entries during that request
  </action>
  <verify>
    Verify non-interactively using TestClient (avoids backgrounding): `uv run python -c "from docbot.main import app; from fastapi.testclient import TestClient; import json; c = TestClient(app); r = c.get('/health'); print(r.json()); assert r.status_code == 200"`. Check that stdout/stderr during the request shows structured JSON log lines with timestamp, level, and message fields. Alternatively, capture logs: `uv run python -c "import logging, io, json; from docbot.logging_config import setup_logging; setup_logging('DEBUG'); from docbot.main import app; from fastapi.testclient import TestClient; c = TestClient(app); c.get('/health')"` and verify the output lines are parseable JSON.
  </verify>
  <done>Every log line is a parseable JSON object with timestamp, level, logger, message. Request middleware logs method, path, status, duration_ms with request_id correlation.</done>
</task>

</tasks>

<verification>
1. `uv run python -c "from docbot.main import app; from fastapi.testclient import TestClient; c = TestClient(app); r = c.get('/health'); assert r.status_code == 200 and r.json()['status'] == 'alive'"` passes
2. `uv run python -c "from docbot.main import app; from fastapi.testclient import TestClient; c = TestClient(app); r = c.get('/ready'); assert r.status_code == 200"` passes
3. Stdout shows JSON log lines for each request (timestamp, level, message fields)
4. `DOCBOT_ENV=test` loads config.test.json; `DOCBOT_ENV=prod` loads config.prod.json
5. Missing config file produces warning but app still starts with defaults
</verification>

<success_criteria>
- FastAPI app runs with UV
- Config system loads env-specific JSON files with complete schema
- All logs are structured JSON with request correlation
- Health and readiness endpoints respond correctly
- .gitignore prevents config files with secrets from being committed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
